# d+m Clip Explorer — v4

## Before going live, edit src/App.jsx

Find these two lines near the top and fill them in:

```
const API_URL       = "https://your-app.railway.app";
const TEAM_PASSWORD = "yourteampassword";
```

Until you do, the app runs in DEMO MODE (red banner) on mock data.

## Netlify build settings

- Build command:   npm run build
- Publish directory: build
